global:
  scrape_interval: 10s
  evaluation_interval: 30s

scrape_configs:
  - job_name: voicerag
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]

rule_files:
  - /etc/prometheus/alerts.yml
